/// <reference types="vite/client" />

// Resolved by the Vite config to `src/components/index.ts`, or to `src/components/control.ts` for the Control.
declare module 'bench:components' {
  export const loadsImpulse: boolean;
}
