// Imported by the page through the `bench:components` alias. The Control aliases it to `./control.ts` instead, so its
// bundle has no Impulse and every component tag stays undefined.
import './app_page.ts';
import './attr_host.ts';
import './data_table.ts';
import './table_row.ts';
import './watcher_host.ts';

export const loadsImpulse = true;
